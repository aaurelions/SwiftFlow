import Foundation

/// The visual style of the temporary connection line drawn while creating an edge.
///
/// This is a type alias for `EdgeType`, where
/// `ConnectionLineType` and edge types share the same visual styles.
public typealias ConnectionLineType = EdgeType
